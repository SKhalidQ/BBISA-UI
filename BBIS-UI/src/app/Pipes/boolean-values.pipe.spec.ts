import { BooleanValuesPipe } from './boolean-values.pipe';

describe('BooleanValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
